@model rivarola_riquelme_rastrilla.Models.Contratos

@{
    ViewData["Title"] = "Asignar Multa";
}

<h1>@ViewData["Title"]</h1>

<form asp-action="Guardar" method="post">
    <input type="hidden" asp-for="Id" />

    <div class="form-group">
        <label asp-for="Inquilino_dni">Inquilino</label>
        <select asp-for="Inquilino_dni" class="form-control" disabled>
            <option value="">Seleccione un inquilino</option>
            @foreach (var inquilino in ViewBag.Inquilinos)
            {
                <option value="@inquilino.Dni">@inquilino.Dni</option>
            }
        </select>
        <span asp-validation-for="Inquilino_dni" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="Inmueble_id">Inmueble</label>
        <select asp-for="Inmueble_id" class="form-control" disabled>
            <option value="">Seleccione un inmueble</option>
            @foreach (var inmueble in ViewBag.Inmuebles)
            {
                <option value="@inmueble.Id">@inmueble.Direccion</option>
            }
        </select>
        <span asp-validation-for="Inmueble_id" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="Estado">Estado del Contrato</label>
        <select asp-for="Estado" class="form-control" disabled>
            <option value="0">Activo</option>
            <option value="1">Finalizado</option>
            <option value="2">Cancelado</option>
        </select>
        <span asp-validation-for="Estado" class="text-danger"></span>
    </div>


    <div class="form-group">
        <label asp-for="Monto">Monto de la multa</label>
        <input asp-for="Monto" class="form-control" />                                                                      
        <span asp-validation-for="Monto" class="text-danger"></span>
    </div>

    <br>
    <button type="submit" class="btn btn-custom">Multar</button>
</form>