@model InquilinosViewModel

@{
    ViewData["Title"] = "Listado de personas";
}

<link href="~/css/pagination.css" rel="stylesheet" />

<div>
    <div class="display-5">Lista de Inquilinos</div>
</div>

<h3>
    <a href="/Inquilino/AltaInquilino"  class="btn btn-custom">Agregar Inquilino</a>
</h3>
<table class="table table-striped table-hover text-center">
    <thead>
        <tr>
            <th>Dni</th>
            <th>Nombre</th>
            <th>Apellido</th>
            <th>Telefono</th>
            <th>Email</th>
            <th>Accion</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Inquilinos != null)
            @foreach (var Inquilino in Model.Inquilinos)
            {
                <tr>
                    <td>
                        @Inquilino.Dni
                    </td>
                    <td>
                        @Inquilino.Nombre
                    </td>
                    <td>
                        @Inquilino.Apellido
                    </td>
                    <td>
                        @Inquilino.Telefono
                    </td>
                    <td>
                        @Inquilino.Email
                    </td>
                    <td>
                        @if(User.IsInRole("Administrador"))
                        {
                            <a href="/Inquilino/Delete?Id=@Inquilino.Id" class="me-2" ><i class="fa-solid fa-trash ic-sz ic-color"></i></a>
                        }
                        <a href="/Inquilino/Editar?Id=@Inquilino.Id" class="me-2"><i class="fa-solid fa-pencil ic-sz ic-color"></i></a>
                        <a href="/Inquilino/Details?Dni=@Inquilino.Dni"><i class="fa-solid fa-circle-info ic-sz ic-color"></i></a>
                    </td>
                </tr>
            }
    </tbody>
</table>
<div class="pagination">
    <!-- Botón "Primera página" -->
    @if (Model.CurrentPage > 1)
    {
        <a href="/Inquilino/Index?pagina=1" class="btn btn-custom">Primera</a>
    }

    <!-- Páginas dinámicas -->
    @{
        int startPage = Math.Max(1, Model.CurrentPage - 1);
        int endPage = Math.Min(Model.TotalPages, Model.CurrentPage + 1);

        for (int i = startPage; i <= endPage; i++)
        {
            if (i == Model.CurrentPage)
            {
                <span class="btn btn-custom active">@i</span>
            }
            else
            {
                <a href="/Inquilino/Index?pagina=@i" class="btn btn-custom">@i</a>
            }
        }
    }

    <!-- Botón "Última página" -->
    @if (Model.CurrentPage < Model.TotalPages)
    {
        <a href="/Inquilino/Index?pagina=@Model.TotalPages" class="btn btn-custom">Última</a>
    }
</div>
<div aria-live="polite" aria-atomic="true" class="position-fixed top-0 end-0 p-3" style="z-index: 1100">
    <div id="toastContainer"></div>
</div>

<script>
    document.addEventListener("DOMContentLoaded", function () {
        @if (TempData["ToastMessage"] != null)
        {
            <text>
            showToast("@TempData["ToastMessage"]", "@TempData["ToastType"]");
            </text>
        }
    });

    function showToast(message, type) {
        const toastId = `toast-${Date.now()}`; // ID único para el toast
        const toastHtml = `
            <div id="${toastId}" class="toast align-items-center text-bg-${type} border-0" role="alert" aria-live="assertive" aria-atomic="true">
                <div class="d-flex">
                    <div class="toast-body">
                        ${message}
                    </div>
                    <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
                </div>
            </div>
        `;

        const toastContainer = document.getElementById('toastContainer');
        toastContainer.insertAdjacentHTML('beforeend', toastHtml);

        const toastElement = document.getElementById(toastId);
        const bootstrapToast = new bootstrap.Toast(toastElement);
        bootstrapToast.show();

        // Eliminar el toast del DOM cuando se oculta
        toastElement.addEventListener('hidden.bs.toast', () => {
            toastElement.remove();
        });
    }
</script>