@model rivarola_riquelme_rastrilla.Models.Tipo

@{
    ViewData["Title"] = "Detalles del Tipo";
}

<h2>Detalles del Tipo</h2>

<div>
    <h4>Tipo</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Nombre)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Nombre)
        </dd>
    </dl>
    <div>
        <a href="@Url.Action("Editar", new { id = Model.Id })" class="btn btn-custom">Editar</a>
        <a href="@Url.Action("Index")" class="btn btn-secondary">Volver al Listado</a>
    </div>
</div>
